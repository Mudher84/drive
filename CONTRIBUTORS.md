# Contributors

## المشروع الأصلي

Telegram Drive — مُنشأ بواسطة caamer20 (GitHub: https://github.com/caamer20/Telegram-Drive).
جميع حقوق الكود الأصلي محفوظة لمؤلفه.

## تعديلات ومساهمات هذه النسخة

بواسطة: albayati

- 2026-07-30 — إكمال الترجمة العربية (`app/src/i18n/locales/ar.json`): ترجمة 289 نصاً كانت متبقية بالإنجليزية، إصلاح خطأ تحقق (مفتاح زائد)، إضافة صيغة الجمع المثنى العربية الناقصة لنص التشفير، إضافة "WebDAV" لقائمة الاستثناءات في `app/src/i18n/invariant-allowlist.json`. تم التحقق من الملف عبر أداة `npm run i18n:validate` الرسمية للمشروع (0 أخطاء، 0 تحذيرات).
- 2026-07-30 — إزالة جميع الإعلانات من التطبيق: حذف بوابة الإعلان بعد تسجيل الدخول (`AdGateway`) من `App.tsx`، وشريط Adsterra من نسخة الجوال (`MobileDashboard.tsx`)، وبانر الإعلان من نسخة سطح المكتب (`DesktopDashboard.tsx`). أصبح الدخول للتطبيق بعد تسجيل الدخول مباشراً بدون أي محتوى إعلاني.
- 2026-07-30 — المرحلة الأولى من إعادة التصميم المطابقة لقالب Boxify (نظام الألوان): إضافة سمة جديدة مدمجة باسم "Boxify" (`app/src/theme/presets.ts`) بلوحة ألوان فاتحة تطابق القالب (خلفية رمادية فاتحة، بطاقات بيضاء، شريط جانبي أزرق نيلي غامق `#3f51b5`، لون تمييز أزرق سماوي `#03a9f4`). توسيع محرك السمات (`app/src/theme/themeEngine.ts`) بمتغيرات جديدة مخصصة للشريط الجانبي (`sidebarBg`, `sidebarText`, `sidebarTextMuted`, `sidebarActiveBg`, `sidebarBorder`) بحيث يمكن للشريط الجانبي أن يأخذ هوية لونية مستقلة عن بقية الواجهة، مع الحفاظ على شكل جميع السمات القديمة كما هي. إضافة متغيرات CSS مقابلة في `app/src/App.css`. تحديث الشريط الجانبي (`Sidebar.tsx`, `SidebarItem.tsx`) لاستخدام هذه المتغيرات الجديدة بدل الألوان العامة. هذه المرحلة تغطي نظام الألوان فقط؛ إعادة تصميم شاشة الدخول والبطاقات والجداول ستأتي في مراحل لاحقة.

- 2026-07-30 — إزالة روابط التبرعات (PayPal/LTC/BTC) الخاصة بالمطوّر الأصلي من داخل التطبيق (`AuthWizard.tsx` — زر "Donate" ونافذته المنبثقة بالكامل) ومن `README.md`.

- 2026-07-30 — إضافة صورة الحساب في الشريط الجانبي وإشعارات سطح المكتب لرسائل تلغرام الفعلية (ميزتان جديدتان حقيقيتان، أول مسودة تحتاج بناء واختبار):
  - **صورة الحساب:** أمر Rust جديد `cmd_get_self_profile` (`app/src-tauri/src/commands/profile.rs`) يجلب الاسم واسم المستخدم ويحمّل صورة الحساب من تلغرام ويخزّنها محلياً؛ خطاف واجهة جديد `useSelfProfile` (`app/src/hooks/useSelfProfile.ts`) يعرضها في أعلى الشريط الجانبي (`Sidebar.tsx`) مع Fallback بحرف الاسم عند عدم وجود صورة.
  - **إشعارات الرسائل:** مستمع خلفي جديد في `commands/auth.rs` (`spawn_notification_listener`) يستمع لتحديثات تلغرام الحية (`client.next_update()`) ويعرض إشعار سطح مكتب حقيقي لكل رسالة واردة جديدة عبر `tauri-plugin-notification`، مع دورة إيقاف نظيفة عند تسجيل الخروج/إعادة الاتصال. تمت إضافة صلاحية `notification:default` وطلب إذن الإشعارات من الواجهة عند تسجيل الدخول.
  - ⚠️ **ملاحظة:** الكود مكتوب اعتماداً على واجهة برمجة grammers القياسية، لكن هذا المشروع يستخدم نسخة معدّلة (fork) منها لم أستطع فحص كودها المصدري من هذه البيئة (لا يوجد وصول لشبكة GitHub هنا). يحتاج الأمر لتشغيل `cargo build` محلياً على جهازك للتأكد من التوافق، وقد تحتاج لتصحيح بسيط بأسماء بعض الدوال إذا ظهرت أخطاء ترجمة (compile errors) — أرسل لي أي خطأ وسأصححه فوراً.

- 2026-07-30 — المرحلة الثانية من إعادة تصميم Boxify: تحويل شاشة تسجيل الدخول (`AuthWizard.tsx`) إلى تخطيط مقسوم (Split-Screen) مطابق للقالب — لوحة جانبية بلون التمييز (Accent) تحمل الشعار واسم التطبيق ووصفاً مختصراً على الشاشات المتوسطة والكبيرة، ونموذج تسجيل الدخول بجانبها. على الشاشات الصغيرة (جوال) يبقى النموذج بشكله المُركزي الأصلي دون تغيير. عناصر النموذج نفسها (الحقول، الأزرار) لم تتغير وظيفياً وتستمد ألوانها تلقائياً من السمة النشطة حالياً (متوافقة مع Boxify والسمات الأخرى).

- 2026-07-30 — زر لغة عربية سريع وزر مظهر سريع (بشاشة الدخول والإعدادات)، وتقليص المظاهر الجاهزة:
  - شاشة الدخول (`AuthWizard.tsx`): زر جديد بجانب زر الوضع الفاتح/الداكن للتبديل السريع بين العربية والإنجليزية (يتحكم بنفس إعداد اللغة الذي تعتمده بقية الواجهة).
  - الإعدادات (`SettingsModal.tsx`، قسم اللغة والمنطقة): زر "العربية" سريع بجانب القائمة المنسدلة الكاملة، وزر تبديل المظهر (فاتح/داكن) بجانبها.
  - تقليص المظاهر الجاهزة (`theme/presets.ts`) من 8 إلى 3 فقط: **Boxify** (الافتراضي الجديد)، Default Dark، Default Light. تم حذف: Charcoal، Nord، Monokai، Cyber Teal، Solarized Light.

- 2026-07-30 — زر تفعيل/تعطيل إشعارات الرسائل من الإعدادات: إضافة `notificationsEnabled` لإعدادات التطبيق، أمر Rust جديد `cmd_set_notifications_enabled` (`commands/settings.rs`) يزامن القيمة مع حالة `NotificationSettings` بالخلفية، ومستمع الإشعارات (`spawn_notification_listener`) أصبح يتحقق من هذا الإعداد قبل عرض أي إشعار. مفعّل افتراضياً.
- 2026-07-30 — تصحيح ملاحظة سابقة: نافذة الإعدادات لا تحتاج تحديث ألوان يدوي لتطابق Boxify — هي أصلاً تقرأ ألوانها من متغيرات CSS (`--color-telegram-*`) التي يبدّلها نظام السمات تلقائياً عند اختيار Boxify. تم التحقق من هذا بفحص الكود مباشرة.
- 2026-07-30 — **تصحيح كامل بخصوص حالة ميزة التشفير:** بعد فتح ملف `commands/fs.rs` فعلياً (163 كيلوبايت — لم أكن قد فتحته من قبل بهذه المحادثة) تبيّن أن ميزة التشفير **مكتملة ومربوطة فعلياً بالرفع والتنزيل**: `cmd_upload_file_encrypted` يولّد مفتاح الملف (DEK) ويغلّفه بـ`wrap_dek` لكل من وضع الخزنة (Vault) ووضع كلمة مرور الملف، ويرفع البيانات عبر `EncryptingReader`؛ وعملية التنزيل تحلل رأس الملف المستلم وتفك تغليف المفتاح عبر `unwrap_dek` ثم تفك التشفير عبر `DecryptReader` أثناء البث المباشر (streaming). الواجهة الأمامية (`useFileUpload.ts`) ترسل وضع الحماية المختار فعلياً لأمر الرفع. الملف المسوّد `/tmp/unwrap_dek_draft.rs` الذي أُرسل سابقاً لا حاجة له إطلاقاً — يمكن تجاهله/حذفه، لأن الكود الحقيقي بالمشروع أفضل وأشمل منه. القيد الحقيقي الوحيد المتبقي: معاينة الملفات المشفّرة داخل التطبيق (thumbnail/preview سريع) غير مدعومة حالياً — يجب تنزيل الملف كاملاً لعرضه، وهذا على الأرجح قرار مقصود من المطوّر الأصلي وليس خللاً.

- 2026-07-30 — المرحلة الثالثة من إعادة تصميم Boxify (البطاقات والجداول): توسيع محرك السمات (`app/src/theme/themeEngine.ts`) ليضع سمة `data-theme-id` على `<html>` عند تفعيل أي سمة مدمجة/مخصصة (ويزيلها عند إلغاء التفعيل)، بحيث يمكن لملف الأنماط استهداف Boxify تحديداً دون التأثير على السمات الأخرى. أُضيفت فئة CSS جديدة `file-card-surface` على بطاقة الملف (`FileCard.tsx`) و`file-table-container` / `file-table-header` على حاوية عرض القائمة ورأسها (`FileExplorer.tsx`) و`file-table-row` على صف الملف في القائمة (`FileListItem.tsx`). في `App.css`، تُطبَّق زوايا دائرية أوسع (`1rem`) وظل ناعم (`box-shadow`) على البطاقات مع ظل أبرز عند التحويم، وزوايا دائرية وظل مماثلين على حاوية الجدول، إضافة إلى رأس جدول بخلفية داكنة قليلاً وأحرف كبيرة (uppercase) — كل ذلك مقيّد بمحدد `:root[data-theme-id="boxify"]` فقط. لم تُلمس ألوان الخلفية أو الحدود الحالية على العناصر نفسها عمداً حتى لا تتعطل حالات التحديد (selection) والتحويم (hover) والسحب (drag-over) التي تعتمد على فئات Tailwind الشرطية الموجودة أصلاً؛ هذه المرحلة تضيف الشكل والارتفاع البصري (elevation) فقط. السمتان الأخريان (Default Dark وDefault Light) غير متأثرتين لأن كل القواعد الجديدة مقيّدة بمعرّف Boxify.

> يُحدَّث هذا الملف مع كل تعديل تصميم أو ميزة جديدة تُضاف لاحقاً في هذه النسخة.
